// Generated by scripts/generate_shared_contract.py; edit shared/protocol.json.
package org.lifelane.mobile

enum class Approach { NORTH, EAST, SOUTH, WEST }
enum class JunctionState { OUTSIDE_COVERAGE, JUNCTION_CANDIDATE, APPROACH_CONFIRMING, APPROACH_CONFIRMED, PRIORITY_REQUESTED, CONTROLLER_VALIDATED, SAFE_TRANSITION, PRIORITY_GREEN, STOP_LINE_CROSSED, INSIDE_JUNCTION, EXIT_CONFIRMING, JUNCTION_CLEARED, NORMAL_RESTORING, COMPLETED, REQUEST_REJECTED, CANCELLED, FAIL_SAFE }
enum class OperatingMode { SIMULATION, HARDWARE_IN_THE_LOOP, LIVE_AMBULANCE }
enum class GpsState { PERMISSION_REQUIRED, LOCATING, ACCURATE, LOW_ACCURACY, LAST_KNOWN, UNAVAILABLE, STOPPED }
enum class ConnectionState { DISCONNECTED, CONNECTING, CONNECTED, INTERRUPTED }
enum class VehicleStage { SPAWNED, APPROACHING, QUEUED, STOPPED_AT_RED, MOVING_ON_GREEN, CROSSING_STOP_LINE, INSIDE_JUNCTION, EXITING, CLEARED, REMOVED }
enum class SignalColour { RED, YELLOW, GREEN }
enum class TripState { IDLE, ACTIVE, CANCELLED, COMPLETED }
enum class PiState { INITIALIZING, READY, UNAVAILABLE, FAIL_SAFE }
